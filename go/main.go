package main

import (
	"fmt"
	"math/rand"
	"time"
)

func main() {
	//clock := time.Now().Clock()
	//fmt.Println(clock)
	// for i := 0; i < 100; i++ {
	// 	// rand.Intn(10)
	// 	fmt.Println(rand.Intn(11) + 1)
	// 	time.Sleep(100 * time.Millisecond)
	// 	// if (i % 20) == 0 {
	// 	// 	fmt.Println(i)
	// 	// }
	// }
        for i := 0; i < 100; i++ {
          fmt.Println(i % 0)
        }
}
